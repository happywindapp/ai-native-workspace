# Secrets / local config — NEVER commit
server-config.md
.env
.env.*
*.local
secrets.*

# Vendored skill/plugin repos (clone separately or use submodules)
anthropics_skills/
anthropics_knowledge-work-plugins/
mattpocock_skills/

# Personal working notes / reports
plans/reports/*
!plans/reports/.gitkeep

# Office / temp artifacts
*.xlsx
*.docx
*.pptx
*.pdf
*.log
tmp_extract/
tmp/

# Dependencies / build
node_modules/
dist/
build/
.venv/
venv/
__pycache__/
vendor/
target/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
.antigravity/
# NOTE: .agent/ (rules + workflows) IS committed — it's Antigravity template content, not local state.
